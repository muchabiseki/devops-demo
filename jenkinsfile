pipeline {
    agent any

    environment {
        IMAGE_NAME = "muchabiseki/devops-demo"
    }

    stages {

        stage('Checkout') {
            steps {
                cleanWs()
                // Checkout the main branch
                checkout([$class: 'GitSCM', 
                          branches: [[name: 'refs/heads/main']], 
                          userRemoteConfigs: [[url: 'https://github.com/muchabiseki/devops-demo.git']]])
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    def appDir = "devops-demo/app"
                    // Build Docker image using Docker Pipeline plugin
                    dockerImage = docker.build("${env.IMAGE_NAME}:${env.BUILD_NUMBER}", "-f ${appDir}/Dockerfile ${appDir}")
                }
            }
        }

        stage('Push Docker Image') {
            steps {
                script {
                    // Push using Docker credentials stored in Jenkins
                    withDockerRegistry([credentialsId: 'docker-hub-creds', url: 'https://index.docker.io/v1/']) {
                        dockerImage.push()
                    }
                }
            }
        }

        stage('Run Tests') {
            steps {
                script {
                    // Optional: Run Python tests inside the Docker image
                    docker.image("${env.IMAGE_NAME}:${env.BUILD_NUMBER}").inside {
                        sh 'pytest'
                    }
                }
            }
        }

    }

    post {
        always {
            cleanWs()
        }
    }
}